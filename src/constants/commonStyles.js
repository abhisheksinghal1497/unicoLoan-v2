import customTheme from "../colors/theme";

export const fieldContainerStyle = {
  marginHorizontal: 5,
  marginTop: 10,
  paddingBottom: 0,
};

export const fieldLabelViewStyle = {
  flexDirection: "row",
  alignItems: "flex-end",
  marginBottom: 6,
};

export const fieldLabelStyle = {
  //   ...customTheme.fonts.regularText,
  ...customTheme.fonts.regularText,
};
